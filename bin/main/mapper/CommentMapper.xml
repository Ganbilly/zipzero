<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktds.zipzero.comment.mapper.CommentMapper">

  <select id="getCommentList" resultType="com.ktds.zipzero.comment.dto.CommentDTO">
    select
      c_content as ccontent, c_moddate as cmoddate, p_id as pid, m_id as mid
    from
      tbl_comment
    where
      p_id = #{pid} and c_check = '1'
    order by
      c_regdate desc
    limit
      #{skip}, #{size}
  </select>

  <insert id="write">
    INSERT INTO tbl_comment (
      c_content as ccontent,
      c_moddate as cmoddate,
      p_id as pid

    ) VALUES (
      #{c_content},
      NOW(),
      #{p_id}
    )
  </insert>


</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktds.zipzero.payment.mapper.PaymentMapper">

  <select id="getPage" resultType="com.ktds.zipzero.payment.dto.PaymentDTO">
    SELECT *
    FROM tbl_payment pay
    WHERE pay.m_id = #{payment.mid}
    ORDER BY p_id DESC
    LIMIT #{page.skip}, #{page.size}
  </select>

  <select id="getDetail" resultType="com.ktds.zipzero.payment.dto.PaymentDTO">
    select * from tbl_payment where p_id = #{pid}
  </select>

  <update id="deletePayment">
    update tbl_payment set p_check = 0 where p_id = #{pid}
  </update>

  <update id="modifyPayment">
    update
      tbl_payment
    set
      p_name = #{pname}, <!-- 결제내역 -->
      p_time = #{ptime}, <!-- 결제시각 -->
      p_moddate = #{pmoddate}, <!-- 결제건수정시간 -->
      p_storename = #{pstorename}, <!-- 결제상호명 -->
      p_totalprice = #{ptotalprice}, <!-- 결제총액 -->
      p_cardtype = #{pcardtype}, <!-- 결제카드유형 -->
      p_receipt = #{preceipt}, <!-- 영수증 -->
      p_curstate = 2, <!-- 결제 상태 -->
      p_finstate = #{pfinstate}, <!-- 최정 결제 상태 -->
      s_id = #{sid}, <!-- 상태아이디 -->
      ptype_code = #{ptypecode} <!-- 회원아이디 -->
    where
      p_id = #{pid}
  </update>

</mapper>